[project]
name = "drift-genetic-optimizer"
version = "0.1.0"
description = "Genetic algorithm parameter optimization for funding rate strategy"
requires-python = ">=3.10"
dependencies = [
    "deap>=1.4.0",
    "numpy>=1.24.0",
    "pandas>=2.0.0",
]

[tool.setuptools.packages.find]
where = ["."]
