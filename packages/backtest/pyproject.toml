[project]
name = "drift-backtest"
version = "0.1.0"
description = "Backtesting engine for delta-neutral funding rate strategy"
requires-python = ">=3.10"
dependencies = [
    "pandas>=2.0.0",
    "numpy>=1.24.0",
    "matplotlib>=3.7.0",
    "requests>=2.31.0",
    "scipy>=1.11.0",
]

[tool.setuptools.packages.find]
where = ["."]
