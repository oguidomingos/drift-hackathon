{
  "name": "drift-delta-neutral-vault",
  "private": true,
  "version": "0.1.0",
  "description": "Delta-Neutral Funding Rate Vault — Drift Protocol",
  "scripts": {
    "bot": "pnpm --filter keeper-bot start",
    "bot:dev": "pnpm --filter keeper-bot dev",
    "bot:start": "pnpm --filter keeper-bot exec npx ts-node src/index.ts",
    "vault": "pnpm --filter vault-sdk start",
    "test:connection": "pnpm --filter keeper-bot exec npx ts-node src/test-connection.ts",
    "postinstall": "node scripts/patch-grpc.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "pnpm": {
    "overrides": {
      "helius-laserstream-win32-x64-msvc": "file:./stubs/helius-laserstream-win32-x64-msvc"
    }
  }
}
